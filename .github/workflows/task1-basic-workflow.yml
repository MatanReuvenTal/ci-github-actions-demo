name: Task-01-Basic-Workflow
on:
  push:
    branches: [main]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Run a shell command
        run: echo " Hello, CI with GitHub Actions!"
